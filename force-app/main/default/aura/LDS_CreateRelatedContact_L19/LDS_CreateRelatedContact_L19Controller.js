({
	someAction : function(component, event, helper) {
		/*event.preventDefault();
        var recId = component.get("v.recordId");
         var evtFields = event.getParam("fields");
        evtFields["AccountId"] = recId;
        component.find("rec").submit(evtFields);*/
	}
   })